export enum MemoryStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum MemoryRevisionAction {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DEACTIVATE = 'DEACTIVATE',
  REEMBED_REQUESTED = 'REEMBED_REQUESTED',
}
